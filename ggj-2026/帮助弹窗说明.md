# 帮助弹窗系统说明

## 📋 功能概述

游戏主菜单的帮助按钮现已实装完整功能，点击后会弹出全屏帮助说明弹窗。

## 📁 相关文件

### 场景和脚本
- `Scene/HelpPopup.tscn` - 帮助弹窗场景
- `Script/help_popup.gd` - 帮助弹窗控制脚本
- `Script/main_menu.gd` - 主菜单脚本（已更新）

### 图片资源
- `Art/help_img_1.png` ~ `Art/help_img_7.png` - 7张帮助说明图片

## 🎨 界面设计

### 弹窗布局
```
┌─────────────────────────────────────┐
│  [黑色半透明遮罩 - 全屏]            │
│                                     │
│     ┌─────────────────────┐         │
│     │                     │         │
│     │   帮助说明图片      │         │
│     │   (1000x600)        │         │
│     │                     │         │
│     └─────────────────────┘         │
│              1 / 7                  │
│                                     │
│   [◀ 上一张] [关闭] [下一张 ▶]     │
│                                     │
└─────────────────────────────────────┘
```

### 视觉元素
- **遮罩层**: 黑色半透明 (70% 透明度)
- **图片显示**: 居中显示，1000x600 像素
- **页码显示**: 图片下方，白色文字，24号字体
- **按钮样式**: 白色文字，悬停时变为浅蓝色
- **关闭按钮**: 红色文字，突出显示

## 🎮 使用方式

### 用户操作
1. **点击"游戏帮助"按钮** → 弹出帮助弹窗
2. **查看说明图片** → 默认显示第1张
3. **切换图片**:
   - 点击"下一张 ▶"按钮 → 查看下一张
   - 点击"◀ 上一张"按钮 → 查看上一张
   - 使用键盘方向键 ← → 或 A/D 键切换
4. **关闭弹窗**:
   - 点击"关闭"按钮
   - 按 ESC 键

### 图片切换逻辑
- 支持7张图片线性浏览（不循环）
- 在第1张时自动隐藏"上一张"按钮
- 在第7张时自动隐藏"下一张"按钮
- 页码实时显示当前位置（如 "3 / 7"）
- 键盘方向键在首尾页时也不会响应

## 🔧 技术实现

### 场景节点结构
```
HelpPopup (Control - 全屏)
├── Overlay (ColorRect) ← 黑色半透明遮罩
├── HelpImage (TextureRect) ← 图片显示（独立可调）
├── PageLabel (Label) ← 页码显示（独立可调）
├── PrevButton (Button) ← 上一张按钮（独立可调）
├── CloseButton (Button) ← 关闭按钮（独立可调）
└── NextButton (Button) ← 下一张按钮（独立可调）
```

**✨ 新特性**：所有元素都是独立的，可以在 Godot 编辑器中自由拖动调整位置！

### 脚本功能

#### 图片加载
```gdscript
# 在 _ready() 中预加载所有图片
for i in range(1, TOTAL_IMAGES + 1):
    var img_path := "res://Art/help_img_%d.png" % i
    var texture := load(img_path) as Texture2D
    help_images.append(texture)
```

#### 图片切换
```gdscript
# 上一张（不循环）
func _on_prev_button_pressed() -> void:
    if current_index > 1:
        current_index -= 1
        _update_display()

# 下一张（不循环）
func _on_next_button_pressed() -> void:
    if current_index < TOTAL_IMAGES:
        current_index += 1
        _update_display()

# 按钮显示/隐藏控制
func _update_display() -> void:
    # 第1张时隐藏"上一张"按钮
    prev_button.visible = (current_index > 1)
    # 第7张时隐藏"下一张"按钮
    next_button.visible = (current_index < TOTAL_IMAGES)
```

#### 弹窗关闭
```gdscript
func _on_close_button_pressed() -> void:
    popup_closed.emit()  # 发送关闭信号
    queue_free()         # 销毁弹窗
```

### 主菜单集成
```gdscript
# main_menu.gd
func _on_help_button_pressed() -> void:
    # 加载并实例化帮助弹窗
    var help_popup_scene := load("res://Scene/HelpPopup.tscn")
    var help_popup := help_popup_scene.instantiate()
    add_child(help_popup)
```

## ⌨️ 键盘快捷键

| 按键 | 功能 |
|------|------|
| ESC | 关闭弹窗 |
| ← 或 A | 上一张图片 |
| → 或 D | 下一张图片 |

## 🎨 样式自定义

### 修改遮罩透明度
在 `HelpPopup.tscn` 中找到 Overlay 节点：
```
color = Color(0, 0, 0, 0.7)  # 最后一个值是透明度 (0-1)
```

### 修改按钮颜色
```gdscript
# 普通按钮
theme_override_colors/font_color = Color(1, 1, 1, 1)          # 白色
theme_override_colors/font_hover_color = Color(0.8, 0.9, 1, 1) # 浅蓝

# 关闭按钮
theme_override_colors/font_color = Color(1, 0.3, 0.3, 1)      # 红色
theme_override_colors/font_hover_color = Color(1, 0.5, 0.5, 1) # 浅红
```

### 修改图片尺寸和位置
在 Godot 编辑器中打开 `HelpPopup.tscn`，选择 HelpImage 节点：
```
# 在检查器中修改 offset 值
offset_left: -500    # 左边界（负值=向左）
offset_top: -300     # 上边界（负值=向上）  
offset_right: 500    # 右边界（正值=向右）
offset_bottom: 300   # 下边界（正值=向下）

# 示例：更大的图片 (1200x700)
offset_left: -600, offset_top: -350
offset_right: 600, offset_bottom: 350
```

**💡 提示**：也可以直接在编辑器视口中拖动图片边缘调整大小！

详细的位置调整教程请参考 `帮助弹窗-位置调整指南.md`

## 📊 信号系统

### popup_closed 信号
弹窗关闭时会发送 `popup_closed` 信号，可以在主菜单中监听：

```gdscript
func _on_help_button_pressed() -> void:
    var help_popup := help_popup_scene.instantiate()
    add_child(help_popup)
    
    # 连接关闭信号
    help_popup.popup_closed.connect(_on_help_popup_closed)

func _on_help_popup_closed() -> void:
    # 弹窗关闭后的处理（如恢复音乐、保存进度等）
    print("帮助弹窗已关闭")
```

## 🔍 故障排除

### 问题：图片无法显示
**解决方案**:
1. 检查图片文件是否存在于 `Art` 文件夹
2. 确认文件命名为 `help_img_1.png` ~ `help_img_7.png`
3. 在 Godot 编辑器中查看是否正确导入
4. 检查控制台是否有加载错误提示

### 问题：弹窗没有弹出
**解决方案**:
1. 检查 `Scene/HelpPopup.tscn` 文件是否存在
2. 查看控制台是否有加载错误
3. 确认主菜单脚本已正确更新

### 问题：按钮点击无响应
**解决方案**:
1. 确认按钮信号已连接（在 `_ready()` 中）
2. 检查按钮的 `mouse_filter` 属性是否正确
3. 查看控制台是否有错误信息

## 🚀 扩展建议

### 1. 添加音效
```gdscript
# 在按钮点击时播放音效
func _on_next_button_pressed() -> void:
    $ClickSound.play()  # 添加音效节点
    current_index += 1
    _update_display()
```

### 2. 添加切换动画
```gdscript
func _update_display() -> void:
    # 淡出效果
    var tween = create_tween()
    tween.tween_property(help_image, "modulate:a", 0.0, 0.1)
    await tween.finished
    
    # 更换图片
    help_image.texture = help_images[current_index - 1]
    
    # 淡入效果
    tween = create_tween()
    tween.tween_property(help_image, "modulate:a", 1.0, 0.1)
```

### 3. 添加滑动手势支持
```gdscript
var _drag_start: Vector2 = Vector2.ZERO

func _input(event: InputEvent) -> void:
    if event is InputEventScreenDrag:
        var drag = event as InputEventScreenDrag
        if drag.relative.x > 50:  # 向右滑动
            _on_prev_button_pressed()
        elif drag.relative.x < -50:  # 向左滑动
            _on_next_button_pressed()
```

### 4. 记住上次浏览位置
```gdscript
# 使用 ConfigFile 保存进度
func _on_close_button_pressed() -> void:
    var config = ConfigFile.new()
    config.set_value("help", "last_page", current_index)
    config.save("user://help_progress.cfg")
    queue_free()

func _ready() -> void:
    var config = ConfigFile.new()
    if config.load("user://help_progress.cfg") == OK:
        current_index = config.get_value("help", "last_page", 1)
    _update_display()
```

## 📝 图片要求说明

### 推荐规格
- **格式**: PNG (支持透明背景)
- **尺寸**: 1000 x 600 像素
- **数量**: 7 张
- **命名**: help_img_1.png ~ help_img_7.png
- **存放位置**: `Art` 文件夹

### 内容建议
1. **第1张**: 游戏概述和目标
2. **第2张**: 基本操作说明
3. **第3张**: 怀疑值系统介绍
4. **第4张**: 面具系统说明
5. **第5张**: 敌人类型介绍
6. **第6张**: 进阶技巧
7. **第7张**: 制作团队信息

---

**创建日期**: 2026-01-31  
**版本**: 1.0  
**状态**: ✅ 完全实装
